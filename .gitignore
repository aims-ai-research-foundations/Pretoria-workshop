# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# Unlike the pilot repo, frozen slide decks (*.pptx) ARE committed here, inside each session folder.
# Videos and archives still live on Google Drive (they bloat history).
*.mp4
*.mov
*.zip
